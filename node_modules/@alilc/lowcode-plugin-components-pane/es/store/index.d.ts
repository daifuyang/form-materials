import { SnippetMeta } from '@/utils/transform';
export default class ComponentManager {
    snippets: Map<string, SnippetMeta>;
    setSnippets: (snippets: SnippetMeta[]) => void;
    getSnippetById: (id: string) => SnippetMeta;
}
